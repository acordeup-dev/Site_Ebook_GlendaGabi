<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>5 Formatos de Conteúdo que Vendem Sem Vender</title>
  <style>
    :root{
      --bg: #0f1f17;
      --card: rgba(255,255,255,0.06);
      --stroke: rgba(255,255,255,0.10);
      --text: #ffffff;
      --muted: rgba(255,255,255,0.80);
      --lime: #7CFF6B; /* verde limão predominante */
      --shadow: 0 18px 50px rgba(0,0,0,0.35);
      --radius: 16px;
    }

    * { box-sizing: border-box; }

    body{
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
      background: radial-gradient(900px 500px at 15% 10%, rgba(124,255,107,0.20), transparent 60%),
                  radial-gradient(700px 400px at 90% 30%, rgba(124,255,107,0.12), transparent 55%),
                  var(--bg);
      color: var(--text);
    }

    .wrap{
      max-width: 860px;
      margin: 0 auto;
      padding: 56px 20px;
    }

    .hero{
      border: 1px solid var(--stroke);
      background: linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.04));
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      overflow: hidden;
      position: relative;
    }

    .hero::before{
      content:"";
      position:absolute;
      inset:-2px;
      background:
        radial-gradient(420px 240px at 20% 0%, rgba(124,255,107,0.45), transparent 60%),
        radial-gradient(420px 240px at 80% 20%, rgba(124,255,107,0.25), transparent 60%);
      opacity: .55;
      pointer-events:none;
    }

    .hero-inner{
      position: relative;
      padding: 44px 36px;
    }

    .badge{
      display:inline-flex;
      gap:10px;
      align-items:center;
      padding: 10px 14px;
      border-radius: 999px;
      border: 1px solid rgba(124,255,107,0.35);
      background: rgba(124,255,107,0.10);
      color: var(--lime);
      font-weight: 700;
      letter-spacing: 0.2px;
      margin-bottom: 18px;
    }

    h1{
      margin: 0 0 12px;
      font-size: 44px;
      line-height: 1.1;
      color: var(--lime);
      letter-spacing: -0.5px;
    }

    .sub{
      margin: 0 0 26px;
      font-size: 18px;
      line-height: 1.6;
      color: var(--muted);
      max-width: 62ch;
    }

    .grid{
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 22px;
      margin-top: 26px;
    }

    @media (max-width: 900px){
      .grid{ grid-template-columns: 1fr; }
      h1{ font-size: 36px; }
    }

    .card{
      border: 1px solid var(--stroke);
      background: var(--card);
      border-radius: var(--radius);
      padding: 22px;
    }

    .card h2{
      margin: 0 0 12px;
      font-size: 20px;
      color: var(--lime);
    }

    p, li{
      font-size: 16px;
      line-height: 1.65;
      color: rgba(255,255,255,0.92);
    }

    ul{
      margin: 10px 0 0;
      padding-left: 18px;
    }

    li{ margin: 7px 0; }

    .highlight{
      border-left: 4px solid var(--lime);
      padding: 12px 14px;
      background: rgba(124,255,107,0.08);
      border-radius: 10px;
      color: rgba(255,255,255,0.92);
      margin-top: 14px;
    }

    .form-wrap{
      display:flex;
      flex-direction: column;
      gap: 12px;
      margin-top: 8px;
    }

    label{
      font-size: 13px;
      color: rgba(255,255,255,0.75);
      margin-bottom: 6px;
      display:block;
    }

    input{
      width: 100%;
      padding: 14px 14px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(0,0,0,0.18);
      color: var(--text);
      outline: none;
      font-size: 16px;
    }

    input:focus{
      border-color: rgba(124,255,107,0.55);
      box-shadow: 0 0 0 4px rgba(124,255,107,0.12);
    }

    .btn{
      width: 100%;
      padding: 14px 16px;
      border-radius: 12px;
      border: none;
      cursor: pointer;
      background: var(--lime);
      color: #0f1f17;
      font-weight: 800;
      font-size: 16px;
      transition: transform .04s ease-in-out, filter .15s ease-in-out;
    }
    .btn:active{ transform: translateY(1px); }
    .btn:hover{ filter: brightness(0.95); }

    .fine{
      font-size: 13px;
      color: rgba(255,255,255,0.70);
      margin-top: 10px;
      line-height: 1.5;
    }

    .section-title{
      margin: 26px 0 10px;
      font-size: 18px;
      color: var(--lime);
      font-weight: 800;
    }

    .divider{
      height: 1px;
      background: rgba(255,255,255,0.10);
      margin: 18px 0;
    }

    .footer{
      margin-top: 18px;
      font-size: 13px;
      color: rgba(255,255,255,0.55);
      text-align: center;
    }
  </style>
</head>

<body>
  <div class="wrap">
    <section class="hero">
      <div class="hero-inner">
        <div class="badge">📘 E-book gratuito</div>
        <h1>5 Formatos de Conteúdo que Vendem Sem Vender</h1>
        <p class="sub">
          Crie conteúdos que geram conexão, autoridade e vendas — mesmo sem parecer forçado —
          usando um método simples e aplicável.
        </p>

        <div class="grid">
          <div class="card">
            <h2>Você já sentiu que…</h2>
            <ul>
              <li>Posta com frequência, mas quase não vende?</li>
              <li>Quando tenta vender, parece forçado?</li>
              <li>Seu conteúdo é bonito, mas não gera resultado?</li>
            </ul>

            <div class="highlight">
              <strong>O problema não é falta de conteúdo.</strong><br />
              É falta de intenção estratégica.
            </div>

            <div class="divider"></div>

            <h2>O que é este e-book?</h2>
            <p>
              Um guia prático que te mostra como usar 5 formatos de conteúdo para vender com leveza,
              sem empurrar ofertas.
            </p>

            <div class="section-title">O que você vai aprender</div>
            <ul>
              <li>Os 5 formatos que vendem sem parecer venda</li>
              <li>Como gerar conexão com bastidores</li>
              <li>Como mostrar prova sem exagerar</li>
              <li>Como se posicionar no seu nicho</li>
              <li>Como fazer ofertas leves que convertem</li>
            </ul>

            <div class="section-title">Para quem é</div>
            <ul>
              <li>Empreendedores</li>
              <li>Social media iniciantes</li>
              <li>Pequenos negócios</li>
              <li>Infoprodutores</li>
              <li>Prestadores de serviço</li>
            </ul>

            <div class="highlight">
              <em><strong>Não é sobre postar mais.</strong> É sobre postar com intenção.</em>
            </div>
          </div>

          <div class="card">
            <h2>Baixe gratuitamente</h2>

            <!-- Formulário VISUAL. Substitua pelo embed da sua ferramenta (MailerLite/Brevo/LeadLovers) -->
            <form class="form-wrap" onsubmit="event.preventDefault(); alert('Formulário de exemplo. Conecte à sua ferramenta de e-mail.');">
              <div>
                <label for="nome">Nome</label>
                <input id="nome" name="nome" type="text" placeholder="Seu nome" required />
              </div>

              <div>
                <label for="email">E-mail</label>
                <input id="email" name="email" type="email" placeholder="Seu melhor e-mail" required />
              </div>

              <button class="btn" type="submit">Quero receber o e-book gratuito</button>
              <div class="fine">📩 Você receberá o material diretamente no seu e-mail.</div>
            </form>

            <div class="divider"></div>

            <h2>Garantia simbólica</h2>
            <ul>
              <li>Conteúdo direto ao ponto</li>
              <li>Sem enrolação</li>
              <li>Aplicável hoje</li>
            </ul>
            <p class="fine">Você consegue ler em <strong>30 minutos</strong> e aplicar em <strong>7 dias</strong>.</p>
          </div>
        </div>

        <div class="footer">
          © <span id="year"></span> • Método dos 5 Formatos
        </div>
      </div>
    </section>
  </div>

  <script>
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
